# JARVIS + Grok — phone build

See `PHONE_BUILD.md` for the exact phone-only build process.

The `.github/workflows/build-apk.yml` workflow builds a debug APK and uploads it as a downloadable artifact.
